import {memo} from 'react';
import {ContentPanel, ContentPanelTitle} from '^layouts/ContentLayout';

export const UsersAndTeams = memo(() => {
    return (
        <ContentPanel>
            <ContentPanelTitle>Users & Teams</ContentPanelTitle>
        </ContentPanel>
    );
});
