import {memo} from 'react';
import {ContentPanel, ContentPanelTitle} from '^layouts/ContentLayout';

export const SpendByCategory = memo(() => {
    return (
        <ContentPanel>
            <ContentPanelTitle>Spend By Category</ContentPanelTitle>
        </ContentPanel>
    );
});
