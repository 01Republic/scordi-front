import {memo} from 'react';
import {ContentPanel, ContentPanelTitle} from '^layouts/ContentLayout';

export const UpcomingRenewals = memo(() => {
    return (
        <ContentPanel>
            <ContentPanelTitle>Upcoming Renewals</ContentPanelTitle>
        </ContentPanel>
    );
});
