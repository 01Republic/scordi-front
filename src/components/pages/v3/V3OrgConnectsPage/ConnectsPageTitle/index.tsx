import React, {memo} from 'react';

export const ConnectsPageTitle = memo(() => {
    return (
        <section className="mb-6 flex justify-between flex-col md:flex-row">
            <h1>데이터소스</h1>
        </section>
    );
});
