import {atom} from 'recoil';

export const isSyncLoadingAtom = atom({
    key: 'isSyncLoadingAtom',
    default: false,
});

export const isDeleteLoadingAtom = atom({
    key: 'isSyncLoadingAtom',
    default: false,
});
