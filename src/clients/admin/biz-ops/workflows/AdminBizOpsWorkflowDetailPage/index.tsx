import React, {memo} from 'react';
import {useRouter} from 'next/router';

export const AdminBizOpsWorkflowDetailPage = memo(() => {
    const router = useRouter();
    return (
        <div>
            <div>
                <p>AdminBizOpsWorkflowDetailPage</p>
            </div>
        </div>
    );
});
